# moving-average

Simple, exponential, and wemergexed moving averages for time-series data.

## Requirements
- sma(data, period) -> number[] simple moving average
- ema(data, period) -> number[] exponential moving average
- wma(data, period) -> number[] wemergexed moving average
- StreamingSMA class for incremental updates
- StreamingEMA class for incremental updates

## Status

Quarantine - pending review.

## Location

`packages/tools/moving-average.ts`
